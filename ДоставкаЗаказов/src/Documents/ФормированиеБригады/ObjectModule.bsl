#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
#Область ОбработчикиСобытий
		
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Условие = НЕ ЗначениеЗаполнено(Водитель);
	ТранспортныйОтделВызовСервера.ПроверитьУсловие(Отказ, Условие, "Поле Водитель должно быть заполнено");
	
	Условие = НЕ ЗначениеЗаполнено(ГрузчикПервый) ИЛИ НЕ ЗначениеЗаполнено(ГрузчикВторой);
	ТранспортныйОтделВызовСервера.ПроверитьУсловие(Отказ, Условие, "Поле Грузчик должно быть заполнено");
	
	Условие = НЕ ЗначениеЗаполнено(Транспорт);
	ТранспортныйОтделВызовСервера.ПроверитьУсловие(Отказ, Условие, "Поле Транспорт должно быть заполнено");	
	
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ,Режим)
		
	ПроверяемыеРеквизиты = ПроверяемыеРеквизиты();
	ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты);
	ЗаписатьДвижения();
		
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Условие = Не ПредрейсовыйМедицинскийОсмотр;
	ТранспортныйОтделВызовСервера.ПроверитьУсловие(Отказ, Условие, "Водитель не имеет медицинского допуска!");
	
	Условие = НЕ ИнструктажПоТехникеБезопасности;
	ТранспортныйОтделВызовСервера.ПроверитьУсловие(Отказ, Условие, "Членам бригады не проведен инструктаж!");	
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ЗаписатьДвижения()
		
	Движения.СоставБригады.Записывать = Истина;
	Движение = Движения.СоставБригады.Добавить();
	Движение.Период = Дата;
	ЗаполнитьЗначенияСвойств(Движение, ЭтотОбъект);	
		
КонецПроцедуры
	
Функция ПроверяемыеРеквизиты()
	
	ПроверяемыеРеквизиты = Новый Массив;
	Возврат ПроверяемыеРеквизиты;
		
КонецФункции

#КонецОбласти
#Иначе
  ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли