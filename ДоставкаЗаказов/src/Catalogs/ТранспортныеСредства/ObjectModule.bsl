#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
    КонецЕсли;
		
	ПроверкаРегистрационногоНомера(Отказ);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ПроверкаРегистрационногоНомера(Отказ)
	
	Если НЕ СтрПодобнаПоРегулярномуВыражению(РегистрационныйНомер, 
			"[ABCEHKMOPTXY]{1}\d{3}[ABCEHKMOPTXY]{2}\s\d{2,3}....") Тогда

		Текст = "Для заполнения поля регистрационный номер 
				|используйте заглавные буквы латинкого алфавита!";
		ОбщегоНазначенияВызовСервера.СообщениеПользователюСервер(Текст);
		Отказ = Истина;
		
	КонецЕсли;
		
КонецПроцедуры

#КонецОбласти
#Иначе
	ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли